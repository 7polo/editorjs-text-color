/**
 * editorjs-text-color Tool's configuration object that passed through the initial Editor config
 */
export interface TextColorConfig {
    colorCollections: string[],
    defaultColor: string
    pluginType: string
}